- name: notify
  platform: group
  services:
    - service: pushover
#    - service: slack
#    - service: sms
#      data:
#        target:
#          - !secret twilio_target_bryce
- name: pushover
  platform: pushover
  api_key: !secret pushover_api_key
  user_key: !secret pushover_user_key
#- name: slack
#  platform: slack
#  api_key: !secret slack_api_key
#  default_channel: '#home'
#  icon: ':house:'
#- name: sms
#  platform: twilio_sms
#  from_number: !secret twilio_from_number
